# name of compiled binary (without .in/.hex extension)
# default is "main"
TARGET = tx_demo

# Your source files
SRCS = tx_demo.c
SRCS += 

# ---------These paths and dirs are relative to this makefile's path----------
# Yor source (.c/.s) file's location
SRC_DIR = src
# Location of compiled binary
BUILD_DIR = build
# Location of the library
LIBRARY_PATH = ../../lib


# include the main Makefile
include ../Makefile.include
